# Vietnamese Lunch Randomizer

Small React + TypeScript + Vite app to spin a wheel and pick a Vietnamese lunch.

Quick start

1. npm install
2. npm run dev

Build

npm run build

Deploy to Vercel

1. Push this repo to GitHub
2. Import the repo in Vercel and deploy (uses `npm run build`)
